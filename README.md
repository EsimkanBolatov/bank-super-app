# 🏦 Bank Super App (Backend)
**Backend API для банковского суперприложения, разработанный в рамках ITU HACKATHON-2025. Проект реализует концепцию "Super App", объединяя классический банкинг, экосистему сервисов и AI-поддержку.**

## 🛠 Технический стек
1. [ ] Язык: Python 3.12
2. [ ] 
3. [ ] Фреймворк: FastAPI (Asynchronous)
4. [ ] 
5. [ ] База данных: PostgreSQL (Driver: asyncpg)
6. [ ] 
7. [ ] ORM: SQLAlchemy
8. [ ] 
9. [ ] AI Engine: Groq (Llama 3.1) — для мгновенных ответов поддержки
10. [ ] 
11. [ ] Инфраструктура: Docker & Docker Compose
12. [ ] 
13. [ ] Безопасность: JWT, OAuth2, BCrypt, MFA (SMS simulation)

# ⚡ Ключевой функционал
## 🧠 Smart Support (AI): Интеграция с Groq API. Чат-бот понимает контекст (видит баланс пользователя) и отвечает на вопросы.

## 🛡️ Безопасность:

**Двухфакторная аутентификация (MFA).

Блокировка/Разблокировка карт (блокирует транзакции).**

## 💸 Финансы:

**P2P переводы (транзакционная целостность ACID).**

**Открытие счетов и автогенерация карт.**

**Кредитный калькулятор (Mock-скоринг).**

## 🚕 Экосистема:

**Оплата сторонних сервисов (Такси, Кино и т.д.).**

**История операций с категориями.**

## 🚀 Установка и Запуск (Docker) — Рекомендуется
Проект полностью докеризирован. Не нужно устанавливать Python или PostgreSQL вручную.

### 1. Настройка окружения

Создайте файл .env в корне проекта и вставьте туда следующие настройки (включая ключ для AI):

Фрагмент кода

### База данных
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=bank_db
DB_HOST=db
DB_PORT=5432

### Безопасность
SECRET_KEY=super_secret_hackathon_key_2025
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

## Настройки приложения
### Вставьте свой ключ от https://console.groq.com
GROQ_API_KEY=gsk_ваш_ключ_здесь

### 2. Запуск

Выполните команду в терминале:
```
Bash

docker-compose up --build
Примечание: Бэкенд автоматически дождется готовности базы данных (healthcheck), применит миграции и запустится на порту 8000.
```

### 3. Доступ

Swagger UI: http://localhost:8080/docs

Для Frontend (удаленно): Используйте Ngrok для проброса порта 8000.

## 📖 Памятка для Frontend (Данияр)

### 1. Авторизация (Login)

Эндпоинт: POST /auth/login ⚠️ Важно: Данные отправлять как Form Data (x-www-form-urlencoded), а не JSON.

username: Номер телефона (например 87471234567)

password: Пароль

### 2. Блокировка карты

Эндпоинт: PATCH /accounts/{id}/block ⚠️ Важно: В URL передавать ID счета (цифра 1, 2...), а не номер карты (4000...).

### 3. AI Чат

   Эндпоинт: POST /ai/chat Body: { "message": "Сколько у меня денег?" } Ответ придет в поле reply.

###    4. Демонстрация MFA 

   Вызвать POST /mfa/generate.

Посмотреть в консоль сервера (там появится код вида Code: 1234).

Ввести этот код в POST /mfa/verify.

 ## 📂 Структура проекта

    ```
    .
    ├── app/
    │   ├── core/          # Конфигурация (env) и Безопасность
    │   ├── db/            # Модели БД и подключение
    │   ├── routers/       # API Эндпоинты:
    │   │   ├── ai.py          # Логика чата с Groq
    │   │   ├── accounts.py    # Карты и счета
    │   │   ├── transfers.py   # Переводы
    │   │   ├── auth.py        # Вход/Регистрация
    │   │   └── ...
    │   └── main.py        # Точка входа
    ├── alembic/           # Миграции БД
    ├── docker-compose.yml # Конфигурация Docker
    ├── Dockerfile         # Сборка образа
    ├── requirements.txt   # Зависимости
    └── README.md          # Вы здесь
    ```
    
    ---

### ⚠️ Решение частых проблем

Ошибка ConnectionRefusedError при запуске: Подождите 5-10 секунд. Docker настроен на ожидание базы данных, это нормально при первом запуске.

Ошибка 400 в AI чате: Убедитесь, что в .env указан валидный GROQ_API_KEY. Если ошибка про "Model decommissioned" — обновите название модели в app/routers/ai.py на llama-3.1-8b-instant.

Ошибка пароля БД (FATAL: password authentication failed): Если вы меняли пароль в .env, нужно очистить старый том базы: docker-compose down -v Затем запустить заново.